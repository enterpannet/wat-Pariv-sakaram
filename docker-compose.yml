version: '3.8'

services:
  backend:
    build:
      context: ./backend
    container_name: express_server
    environment:
      DATABASE_URL: "postgresql://watRoot:wat123546@210.246.215.231:5432/wat"
    ports:
      - '5000:5000'
    networks:
      - app_network

  db:
    image: postgres:15
    environment:
      POSTGRES_USER: watRoot
      POSTGRES_PASSWORD: wat123546
      POSTGRES_DB: wat
    ports:
      - '5432:5432'
    networks:
      - app_network

networks:
  app_network:
    driver: bridge
